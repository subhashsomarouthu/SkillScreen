python-dotenv==1.0.1
# Optional (identity/emotion):
deepface==0.0.93
face-recognition==1.3.0


# Core
ultralytics>=8.3.0
omegaconf==2.3.0
opencv-python-headless>=4.8
numpy>=1.24

# API (adjust if you're not using FastAPI)
fastapi>=0.110
uvicorn[standard]>=0.23
pydantic>=2.0
python-multipart>=0.0.9

# Database (for interview processing)
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0

# HTTP requests (for video download)
requests>=2.31.0
# Optional: let Ultralytics pull a matching torch wheel automatically.
# If you want to pin CPU-only Torch explicitly, uncomment one of these known-good lines:
# torch==2.2.*+cpu ; platform_system != "Darwin" and python_version >= "3.10"  # via extra index
# torchvision==0.17.*+cpu ; platform_system != "Darwin" and python_version >= "3.10"
